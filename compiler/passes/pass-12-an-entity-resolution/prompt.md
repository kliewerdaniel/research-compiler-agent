# Capability spec: an entity-resolution pass that merges duplicate person/repo nodes in the graph (e.g. "Orinth" vs "Ornith")

Proposed by the self-improvement ledger (auto-selected by
pass-03d-pass-generator).

## Intent
an entity-resolution pass that merges duplicate person/repo nodes in the graph (e.g. "Orinth" vs "Ornith")

## Notes
---

## Acceptance
When this pass runs it should read the relevant existing artifacts, produce
`pass-12-an-entity-resolution-ir`, and (if model_required) emit a concrete artifact that advances
the research loop.
