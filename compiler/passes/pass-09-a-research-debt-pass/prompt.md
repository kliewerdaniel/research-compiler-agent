# Capability spec: a "research debt" pass that tracks posts with low `coverage`/`under_explored_concepts` and proposes consolidation posts

Proposed by the self-improvement ledger (auto-selected by
pass-03d-pass-generator).

## Intent
a "research debt" pass that tracks posts with low `coverage`/`under_explored_concepts` and proposes consolidation posts

## Notes
(no additional notes)

## Acceptance
When this pass runs it should read the relevant existing artifacts, produce
`pass-09-a-research-debt-pass-ir`, and (if model_required) emit a concrete artifact that advances
the research loop.
